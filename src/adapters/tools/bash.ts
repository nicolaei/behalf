// Adapter — the standard `bash` tool binding.

import type { Binding } from "../../flow/tool.js";

/** Standard tool binding that runs a shell command and returns its output. @public */
export declare const bash: Binding;
