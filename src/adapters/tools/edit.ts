// Adapter — the standard `edit` tool binding.

import type { Binding } from "../../flow/tool.js";

/** Standard tool binding that applies line-anchored edits to a file on disk. @public */
export declare const edit: Binding;
