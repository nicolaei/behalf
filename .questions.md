Is there any way to get dynamic graphs based on external data?

For example, let's say an agent creates a set of tasks, that are linked.
Some of these tasks can be done in parallel, and have to be done in sequence.

This is all a graph, but dynamic at runtime.

Could we have some tool that constructs a graph at runtime to spawn agents for each task? 
Maybe even with custom `use`'s for some taks that require something special.

---

Let's write documentation.

4 quadrant docs.

Learn react is a good example of good docs.


---

I want the state machines to have arbitrary input, not just UserMessage.

Come up with a plan for that.


---

Let's create a basic coding agent:

https://huggingface.co/rndubs/code-llm-toolkit
https://modal.com/blog/6-best-code-embedding-models-compared

^ For code agent


---

Let's create a basic writing agent
