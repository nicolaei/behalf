# Wiring a runtime

The production side: supplying model ports and tool bindings, then actually
running a flow.

- [Running flows](./running-flows.md) — `runtime()`, `runFlow()`, the `satisfiesFlows` coverage gate.
- [Model ports and bindings](./model-ports-and-bindings.md) — implementing a `ModelPort` for a provider, assembling tool bindings.

**Up:** [Learn behalf](../README.md)
