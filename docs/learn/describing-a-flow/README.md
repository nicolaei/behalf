# Describing a flow

The vocabulary behind what you just wired: messages, personas, and tools — the `Profile`s and
`Message`s the previous section's examples already used by name.

- [Messages and content](./messages-and-content.md) — `Message`, `ContentBlock`, thinking blocks,
  intents.
- [Profiles and models](./profiles-and-models.md) — `Profile`, `Model`, `ReasoningLevel` — the
  persona a step calls.
- [Tools and handlers](./tools-and-handlers.md) — `tool`/`toolset`, `ToolHandler`,
  `provide`/`expand`, `ToolContext`.

**Up:** [Learn behalf](../README.md)
